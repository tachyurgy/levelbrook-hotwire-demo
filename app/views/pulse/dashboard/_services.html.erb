<%# Streamed service grid (target: pulse_services). locals: services %>
<div id="pulse_services" class="grid gap-3 sm:grid-cols-2">
  <% services.each do |service| %>
    <%= render "pulse/dashboard/service", service: service %>
  <% end %>
</div>
